import React from "react";
import CategorySlider from "./CategorySlider";

const CategoryComponent = (props) => {
  return <CategorySlider className={`${props.className}`} />;
};

export default CategoryComponent;
